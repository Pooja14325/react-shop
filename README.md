# react-shop
ReactShop is a fully responsive e-commerce application built with React. It allows users to browse products, add them to the cart, manage their cart items, and perform authentication (login/logout).
